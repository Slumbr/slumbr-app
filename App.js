// This is the entrypoint for the React Native app. The import statement below
// points to where the "real" entrypoint will be after being compiled by tsc
import { App } from "./build/app/App";

// eslint-disable-next-line import/no-default-export
export default App;
